name: halal
description: تطبيق باركود المنتجات الحلال - تصميم داكن 3D
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  barcode_scan2: ^4.2.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/halal.png
    - assets/haram.png
    - assets/unknown.png
